---
# vars file for setup-website-user-access

# Sites to setup and host on the webserver
remove_website_user_access_list:
  - {
      name: 'mysite'
    }


#####     Optional Variables      #####

# The document root/absolute path that is the parent of the new user's home directory
#remove_website_user_access_web_root: "/var/www"

